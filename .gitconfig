[user]
	name = Billy Zhou
	email = billyzs.728@gmail.com
[credential]
	helper = cache --timeout=36000
[push]
	default = simple
[core]
	autocrlf = input
[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge --skip -- %f
	process = git-lfs filter-process --skip
[color]
	ui = auto
